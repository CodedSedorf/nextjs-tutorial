"use client";
import React from "react";

const AddToCart = () => {
  return (
    <div>
      <button onClick={() => console.log("I got clicked")}>Add to Cart</button>
    </div>
  );
};

export default AddToCart;
